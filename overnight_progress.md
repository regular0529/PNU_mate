# 야간 자율 작업 로그 (2026-08-23 00:48 시작, 06:48 보고 예정)

## 목표
로컬 모델(Qwen3.5 9B/4B)을 간단한 기능으로 활용할 방안을 스스로 찾고, 실험하고, 문제 해결.
사용자는 수면 중 — 질문하지 않고 자율 진행, 6시간 뒤 최종 리포트.

## 00:48 — 완료한 것
1. **9B → 4B 자동 폴백 로직 구현** (`summarize_local()` 함수)
   - 9B로 먼저 시도, 실패/타임아웃 시 4B로 자동 전환, 둘 다 실패하면 None(요약 없이 계속 진행)
   - 이 PC는 여유 RAM이 그때그때 크게 달라져서(2GB~7GB대 요동) 안정성을 위해 필요했음
   - 유닛테스트 3개 작성(mock으로 9B 성공/9B실패→4B성공/둘다실패 케이스), 전부 통과
2. **버그 발견 및 수정**: `log()` 함수가 콘솔이 UTF-8이 아닐 때(cp949 등) 한글 로그 출력하다 크래시하는 문제 발견 → try/except로 방어 처리. 스케줄러 실행 환경에서 실제로 터질 수 있었던 버그라 중요한 발견.
3. 실제 로컬 모델 호출 성공 사례 확보 (앞서 대화에서): "hi" 프롬프트 기준 4B 102.8초, 9B 89.2초 — RAM 압박이 심할 땐 크기 무관하게 다 느림.

## 01:00 — E2E 테스트 결과
- 대기 중이던 실제 요약 테스트 완료됨(성공, exit 0). 응답: `[장학금] 2차 신청 기간`
- 형식(`[분류] 내용`)은 맞게 나왔는데, 지시한 것보다 훨씬 짧음(원래는 "핵심내용 — 왜 중요한지" 상세 요구했는데 축약됨). 샘플 입력이 짧아서 그런 건지, 9B가 지시를 잘 안 따르는 건지 좀 더 확인 필요.
- 다음: 더 현실적인(5개 항목) 샘플로 재확인 중 (여전히 계산 중, RAM 여유가 계속 2GB 밑으로 떨어짐 — 이 시간대에 뭔가 다른 게 메모리를 많이 쓰는 듯).

## 01:20 — 스케줄러 안전성 확인 (문제 없음)
- `PNU_BoardWatcher` 작업의 `MultipleInstances` 정책이 `IgnoreNew` — 실행이 오래 걸려도 다음 트리거가 겹쳐서 중복 실행되지 않음. 안전.
- `LogonType: Interactive` — 사용자 로그온 세션에서만 실행되므로, Ollama의 시작프로그램(Startup 폴더)도 같은 세션에서 이미 떴을 것 → 스케줄 실행 시점에 Ollama가 안 떠있을 걱정 없음.

## 01:25 — 대시보드 회귀 테스트
- 오늘 밤 변경사항(페이지네이션, 마감 스타일, 드래그 순서변경, 로그 인코딩) 전부 육안 확인 — 정상 렌더링.
- 드래그 앤 드롭: 자동화 도구의 마우스 시뮬레이션으로는 네이티브 HTML5 dragstart 이벤트가 안 걸려서 직접 검증은 못 했지만, localStorage 저장/복원 로직(`applySavedOrder`)을 직접 조작해서 확인함 — 순서 반영 정확히 동작. 실제 사람이 마우스로 드래그하면 정상 작동할 것으로 판단(코드 로직 자체는 검증됨).
- 이후 브라우저 스크린샷이 30초 타임아웃 — 시스템이 Ollama 계산으로 많이 눌려서 렌더러 응답이 늦어진 것으로 보임. 리소스 아끼려고 브라우저 테스트는 잠시 중단.

## 다음
- Ollama 응답 계속 대기 (강제 중단 안 함)
- 여유 생기면 텔레그램/별표/즐겨찾기 필터 상호작용도 마저 확인

## 01:42 — 근본 원인 발견: 백그라운드 테스트 타임아웃이 좀비 프로세스를 만듦
- 5개 항목 요약 테스트(bl122agb6)에 제가 타임아웃 700초를 줬는데, 도구 자체 허용 한도(600초)를 넘어서 **하네스가 강제로 작업을 죽임(status: killed)**.
- 그 여파로 `llama-server` 프로세스 하나(PID 11836)가 메모리 -1277MB(비정상값)인 좀비 상태로 남음 — 이게 **여유 RAM을 7GB→1.3GB까지 깎아먹은 진짜 원인**이었음(9B/4B 모델 크기 문제가 아니었음).
- 새 규칙(PID 지정 종료만 허용)에 따라 그 PID 하나만 정리 → 여유 RAM 1.3GB→4.1GB로 즉시 회복됨.
- **교훈**: 앞으로 Ollama처럼 오래 걸리는 로컬 호출을 백그라운드로 돌릴 때, Bash 도구의 자체 타임아웃 상한(600초)을 절대 넘기지 않거나, PowerShell `Start-Process`로 완전히 분리된 프로세스로 띄워서 하네스의 강제종료가 하위 프로세스까지 좀비로 안 만들게 해야 함.
- 지금부터는 완전 분리된 방식으로 재시도.

## 01:41 — 1차 재시도 결과: 실패
- 완전 분리된 프로세스로 돌린 요약 테스트 결과: **9B, 4B 둘 다 타임아웃 실패** (각 600초씩, 총 20분 가까이 걸렸는데도 실패)
- 그 직후 확인하니 RAM이 9.3GB로 자연 회복돼있었음 (모델이 언로드되면서). 즉 테스트 도중엔 RAM이 그만큼 안 좋았다는 뜻.
- **결론(잠정)**: 이 PC는 RAM 여유가 낮은 시간대엔 로컬 모델 응답을 아예 못 받을 수 있음 — 1시간 간격 스케줄이라 다음 시도에서 성공할 가능성 높지만, 100% 신뢰 가능한 기능은 아님. 실패해도 요약 없이 게시판 정보만 넘어가는 현재 설계(graceful degradation)가 적절했음을 재확인.
- 좋은 RAM 조건에서 재시도 진행 중 (성공 사례 하나 더 확보해서 품질 평가 마무리 예정)

## 02:13 — 2차 재시도도 실패, 판단 변경
- RAM 6.6GB로 시작했는데도 9B/4B 둘 다 다시 타임아웃 실패. 총 2번의 "현실적인 5개 항목" 테스트가 전부 실패.
- 성공했던 건 전부 "hi" 같은 한 단어짜리 트리비얼 프롬프트였음. **실제 시스템 프롬프트(장학금/마감일/채용/공모전 분류 지시문, 꽤 김) + 5줄 입력 조합이 이 하드웨어에서 프리필(prompt처리) 자체를 못 견디는 것**으로 보임 — 출력이 짧아도 입력 처리 단계에서 이미 병목.
- 사용자가 무한 재시도 대신 다음 단계(문제 해결/리포트)로 넘어가라고 지시함. 임시파일 정리 완료.

## 02:15 — 문제 해결 시도: 시스템 프롬프트 경량화 실험
- 훨씬 짧은 시스템 프롬프트(한 줄)로 4B/9B 둘 다 다시 테스트 → **역시 둘 다 400초 타임아웃으로 실패**.
- 확인해보니 이번엔 좀비가 아니라 진짜 정상적으로 계산 중이었음(llama-server RAM 1.8GB, 정상 CPU 100%). 그 시점 RAM 여유가 3.9GB로 다시 낮았음.
- **결론**: 프롬프트를 줄여도 실패함 → 병목은 프롬프트 길이가 아니라 **그 순간의 시스템 RAM 여유** 그 자체. 이 변수는 테스트마다 달라서(1.3GB~9.3GB 요동) 프롬프트 길이 효과만 깔끔하게 분리해서 검증하기 어려움.
- 판단: `SUMMARY_SYSTEM_PROMPT`를 짧게 줄이는 변경은 **적용하지 않음** — 효과가 입증 안 됐고, 오히려 요약 품질/형식 지시가 부실해질 리스크만 있음. 원래 상세 프롬프트 유지.
- 실제 콘텐츠(5줄 입력) 기준 **지금까지 4번 시도 전부 실패**, 트리비얼 "hi" 프롬프트만 2번 성공(89~103초). → 이 기능은 "RAM 여유가 충분할 때만 작동하는 보너스 기능"으로 봐야 하며, 실패해도 게시판 정보 자체는 정상 전달되는 현재 설계(graceful degradation)가 핵심 안전장치임을 재확인.

## 02:40 — 새 기능 구현: "신청완료" 표시 + 내 신청 현황 (AI 불필요, 리소스 부담 0)
- 이전 대화에서 나왔던 아이디어(어떤 글에 신청했는지 표시하고 마감일 겹치는지 확인) 를 AI/캘린더 연동 없이 순수 클라이언트 기능으로 구현.
- 게시글마다 ✓ 버튼 추가 → 클릭하면 초록색으로 표시(localStorage 저장, 대시보드 재생성돼도 유지).
- 새 "내 신청 현황" 패널: 체크한 글들을 마감일 파싱해서 D-day 순으로 자동 정렬해서 보여줌 → 여러 개 신청했을 때 마감일이 겹치는지 한눈에 확인 가능.
- 유닛테스트 추가, 브라우저로 실제 클릭까지 E2E 확인함(정상 작동).
- 전체 테스트 스위트(board_watcher/deadlines/everytime_filter) 전부 통과 상태 유지.

## 03:15 — 코드 전체 최종 리뷰
- `board_watcher.py` 1062줄 전체 한 번 더 훑음. 구조적으로 문제 없음.
- 사소한 발견: `main()`의 "(로컬 AI 요약 건너뜀 — ...)" 로그 문자열에 아직 em-dash(—)가 남아있음. 크래시는 이미 `log()` 방어처리로 안 나지만(폴백 처리됨), 굳이 지금 고칠 필요는 없어 보여 그대로 둠(파일 로그엔 영향 없음, 콘솔 표시만 영향, 우선순위 낮음).
- RAM 8.6GB로 좋은 조건에서 3번째 재시도 진행 중 (여전히 계산 중, 강제중단 안함) (백그라운드 실행 중, bl122agb6)

## 01:10 — 코드 리뷰 중 버그 하나 더 발견 및 수정
- `check_boards()`의 멀티페이지(부산대 공지사항 3페이지) 로직: 2/3페이지 요청 중 하나라도 실패하면 예외가 바깥 try로 전파되어 **이미 성공한 1페이지 데이터까지 통째로 버려지고 게시판 전체가 "에러"로 잘못 표시되는 버그** 발견.
- 수정: 추가 페이지 요청만 별도 try/except로 감싸서, 실패하면 경고 로그만 남기고 1페이지 결과는 그대로 유지하도록 변경.
- 회귀 테스트 작성(`test_check_boards_keeps_page1_when_extra_page_fails`), 통과 확인.

## 다음에 시도할 것
- RAM 여유가 회복되는 시간대 있는지 관찰 (한 번에 몰아서 테스트하지 말고 시간 두고 확인)
- 더 가벼운 모델(1.5B급 등)이 있는지 확인해서 최후 폴백 단계로 추가할지 검토 (단, 불필요한 추가 다운로드는 신중히)
- 코드 전체적으로 다른 잠재 버그 없는지 리뷰
