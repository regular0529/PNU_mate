# 야간 자율 작업 리포트 (2026-08-23 00:48 ~ 03:32, 이후 가벼운 모니터링만)

## 한 줄 요약
로컬 AI 요약 연동을 완료했지만 **이 하드웨어에서 신뢰도가 낮다는 걸 확인**했고, 대신 AI 없이 실제로 유용한 기능("신청완료" 마감일 트래커)을 하나 새로 만들었습니다. 버그 2개도 찾아서 고쳤습니다.

---

## 1. 로컬 AI 요약(Qwen3.5 9B/4B) — 연동은 됐지만 신뢰도는 낮음

**한 것:**
- `summarize_local()` 함수로 9B 우선 시도 → 실패/타임아웃 시 4B로 자동 폴백 → 둘 다 실패하면 요약 없이 정상 진행
- 유닛테스트 3개로 폴백 로직 검증 완료

**실측 결과 (총 7회 테스트):**
| 프롬프트 종류 | 결과 |
|---|---|
| 트리비얼("hi") — 2회 | ✅ 성공 (89~103초) |
| 실제 요약 프롬프트(5개 게시글 + 상세 지시문) — 5회 | ❌ 전부 실패 (600초 타임아웃) |

- RAM 여유가 1.3GB일 때도, 9.3GB로 회복됐을 때도, 시스템 프롬프트를 훨씬 짧게 줄였을 때도 전부 실패했습니다. RAM 문제만이 아니라 **이 CPU(Iris Xe 내장그래픽, GPU 가속 없음)가 실제 분량의 프롬프트를 처리하기엔 근본적으로 느리다**는 뜻으로 보입니다.
- **결론: 이 기능은 "되면 좋고 안 돼도 그만"인 보너스로 취급해야 합니다.** 실패해도 게시판 새 글 정보 자체는 정상적으로 대시보드/텔레그램에 전달되도록 이미 안전하게 설계돼 있어서, 당장 불편해지는 건 없습니다.
- **제안**: 나중에 정말 요약 기능을 안정적으로 쓰고 싶으시면, (a) 더 가벼운 모델(1~2B급)을 찾아보거나 (b) Claude API를 다시 고려하거나 (c) 이 기능 자체를 포기하고 키워드 태그(이미 있음: 장학금/채용/공모전/마감임박)로 대체하는 방법이 있습니다. 지금 당장 결정할 필요는 없습니다.

## 2. 버그 2개 발견 및 수정

1. **`log()` 함수 크래시 버그**: 콘솔이 UTF-8이 아닐 때(예: 스케줄러 실행 환경) 한글 로그를 출력하다 통째로 크래시할 수 있었음 → try/except로 방어 처리. 스케줄러가 조용히 죽을 수 있었던 잠재 위험을 미리 제거했습니다.
2. **페이지네이션 데이터 유실 버그**: 부산대 공지사항 게시판이 2/3페이지 중 하나만 실패해도, 이미 성공한 1페이지 데이터까지 통째로 버려지고 게시판 전체가 "에러"로 잘못 표시되던 버그 → 부분 실패 시에도 이미 가져온 데이터는 유지하도록 수정.

두 버그 다 회귀 테스트 작성해서 재발 방지해뒀습니다.

## 3. 새 기능: "신청완료" 마감일 트래커 (AI 불필요, 완전 무료)

이전에 말씀하셨던 "내가 뭐 신청했는지, 마감일 겹치는지" 아이디어를 AI/캘린더 연동 없이 구현했습니다:
- 게시글마다 ✓ 버튼 → 누르면 "신청완료"로 표시(브라우저에 저장, 대시보드 재생성돼도 유지)
- 대시보드 상단에 "내 신청 현황" 패널 추가 — 체크한 글들을 마감일 파싱해서 D-day 순으로 자동 정렬
- 여러 개 신청했을 때 마감일이 겹치는지 한눈에 확인 가능
- 브라우저로 실제 클릭까지 E2E 테스트 완료, 정상 작동 확인

## 4. 그 외 확인/개선한 것들

- **스케줄러 안전성**: `MultipleInstances: IgnoreNew`(중복 실행 방지), `LogonType: Interactive`(Ollama가 항상 같은 세션에서 먼저 떠있음) — 둘 다 안전하게 설정돼 있음을 확인.
- **텔레그램 4096자 제한 청크 분할**: 이전에 발견했던 버그, 정상 작동 재확인.
- **좀비 프로세스 원인 규명**: 백그라운드 테스트에 도구 허용치(600초)보다 긴 타임아웃을 주면 하네스가 강제종료하면서 Ollama 하위 프로세스가 깨진 상태(음수 메모리값)로 남아 시스템 전체가 느려지는 현상을 발견 → PID 지정으로 정리, 이후 완전히 분리된 프로세스 방식으로 테스트해서 재발 방지.
- 드래그 앤 드롭 게시판 순서 변경, 별표/하이라이트, 필터/검색 등 기존 기능 전부 회귀 테스트로 정상 작동 재확인.

## 다음에 정할 것 (지금 당장 결정 불필요)

- 로컬 AI 요약을 계속 이 방식으로 둘지, 다른 모델/방법으로 바꿀지, 포기하고 키워드 태그만 쓸지
- "신청완료" 트래커에 필터 버튼("신청완료만 보기")을 추가할지 (지금은 상단 전용 패널로 충분해 보여서 안 만들었음)
